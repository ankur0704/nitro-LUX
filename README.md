

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` 
3. first create a file `.npm.local`
4. [.env.local](.env.local) to your Gemini API key
5. Run the app:
   `npm run dev`
